<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.kylegilmartin.teamup">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TeamUp">
        <activity android:name=".UserDetails.EditUserProfile"></activity>
        <activity android:name=".UserDetails.UserProfile">

        </activity>
        <activity android:name=".admin.AdminMain" />
        <activity android:name=".LoginRegister.ForgotPassword" />
        <activity android:name=".SplashScreen.SplashScreen" />
        <activity android:name=".LoginRegister.Register" />
        <activity android:name=".LoginRegister.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>